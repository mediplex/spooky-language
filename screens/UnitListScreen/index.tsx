import React from 'react';
import { UnitCard } from '../../components/UnitCard';

export const UnitListScreen = ({ navigation }) => {


  return (
    <UnitCard navigation={navigation} />
  )
}